[package]
name = "portkill"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true

# Fast, single-purpose port inspector and killer for macOS developer workflows.

[[bin]]
name = "portkill"
path = "src/main.rs"

[dependencies]
local-common = { path = "../local-common" }

[lints]
workspace = true

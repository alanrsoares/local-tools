[package]
name = "jwt"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true

# Fast, zero-dependency JWT decoder and claim inspector.

[[bin]]
name = "jwt"
path = "src/main.rs"

[dependencies]
local-common = { path = "../local-common" }

[lints]
workspace = true

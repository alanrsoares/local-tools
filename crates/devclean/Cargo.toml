[package]
name = "devclean"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true

# Blazingly fast multi-ecosystem build artifact scanner and reclaimer.

[[bin]]
name = "devclean"
path = "src/main.rs"

[dependencies]
local-common = { path = "../local-common" }

[lints]
workspace = true
